var Header = React.createClass({
    render() {
        return (
            <div>
                <h1>Hello, this is Rails + react example using react-rails gem!</h1>
            </div>
        )
    }
});